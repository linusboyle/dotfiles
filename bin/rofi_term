#!/bin/bash

#laucher without target hint
#just type in command
RES=$(echo |rofi -dmenu -p '>_:' -mesg "Input the line to exec") 

#quit when any key is pressed
if test -n "$RES" ; then
    urxvt -e bash -c "echo running \\\"\"$RES\"\\\" ...: |lolcat ; $RES ; read -n 1"
fi
