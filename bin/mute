#! /bin/bash

amixer -q set Master toggle 

stat=$(amixer get Master |egrep -o '\[(on|off)\]'|head -1) 
#off=$(amixer get Master |egrep -o '\[off\]'|head -1) 

#printf "$on"
#printf "$off"
if test $stat = "[on]" ; then
    notify-send "Audio Now On"
#elif test $off = "[off]" ; then
else
    notify-send "Audio Now Off"
fi
